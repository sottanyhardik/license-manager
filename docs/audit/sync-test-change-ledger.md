# Durable Synchronization Test-Change Ledger

| Test | Old expectation | New expectation | Requirement changed? | Code defect? | Evidence | Final result |
| --- | --- | --- | --- | --- | --- | --- |
| `apps/core/tests/test_sync_failures.py::{TestReplayAndDuplicates,TestOutOfOrderConvergence}` | An envelope claiming `server-B`/`server-C` could be submitted with `server-A` credentials. | Each event stream is submitted with the credential of its declared registered peer; a mismatched identity remains forbidden. | Yes — peer events are authenticated identities, not browser-user requests. | No | `PeerTokenAuthentication` rejects `source_server != request.auth.server_id`; `TestPushEndpoint.test_push_rejects_a_forged_top_level_source_server`. | 14 focused tests passed |
| `apps/core/tests/test_sync_transport.py::TestSyncFromPeer::test_cursor_acknowledges_the_source_watermark_not_receiver_clock` | Cursor advanced to receiver `now()`. | Cursor acknowledges the response's source watermark. | No | Yes | A source change committed between response snapshot and receiver `now()` must remain eligible for the next delta pull. | Pass |
| `apps/core/tests/test_sync_transport.py::TestSyncFromPeer::test_partial_batch_does_not_acknowledge_the_source_cursor` | Cursor advanced despite rejected inbound events. | Cursor remains unchanged until the whole batch applies without errors. | No | Yes | Replays are idempotent; advancing would permanently lose the rejected event. | Pass |
| `apps/core/tests/test_sync_failures.py::TestEveryRegisteredMasterIsTransportable::test_null_foreign_key_round_trips_as_null` | Pull tried to emit the `ItemNameModel.norms` `ManyRelatedManager` as JSON. | Pull emits only concrete scalar/FK fields; `norms` is excluded. | No | Yes | M2M managers are local relation APIs, not a stable cross-server payload; the independently asserted null FKs still round-trip. | Pass |

Related regression commands: `pytest apps/core/tests/test_sync_transport.py::TestSyncFromPeer -q` (16 passed); `pytest apps/core/tests/test_sync_failures.py::TestReplayAndDuplicates apps/core/tests/test_sync_failures.py::TestOutOfOrderConvergence -q` (14 passed); `DB_NAME=lm_sync_durable pytest apps/core/tests/test_sync_failures.py::TestEveryRegisteredMasterIsTransportable::test_null_foreign_key_round_trips_as_null -q --tb=short --no-cov` (1 passed).
