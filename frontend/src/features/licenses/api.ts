// Re-export the license endpoint constants for convenience within this feature.
// All HTTP calls are made through apiClient in queries.ts / mutations.ts.
export { ENDPOINTS } from '@/shared/api/endpoints'
