# Planning Test-Change Audit Ledger

| Test | Old expectation | New expectation | Requirement changed? | Code defect? | Evidence | Final result |
| --- | --- | --- | --- | --- | --- | --- |
| `apps/license/tests/test_sion_planning_execution_caps.py::{test_force_plan_does_not_bypass_available_quantity_or_cif,test_zero_cif_budget_creates_no_positive_value_plan}` | Fixture represented a rule without `priority`; execution crashed before testing the caps. | Fixture represents a valid persisted rule with priority `1`; 7 available at CIF 50 / price 10 plans exactly 5, and zero CIF plans no positive-value row. | No | No | `SionPlanningRule.priority` is persisted with default 100, is indexed, ordered, and unique among active SION rules. The canonical planner allocates by priority before enforcing availability and Actual Balance CIF. | Pass (focused caps, canonical execution, async replan, and freeze-gate suites: 16 tests) |
| `apps/license/tests/test_sion_planning_execution.py::{test_first_saved_rule_match_is_the_only_owner_of_an_overlapping_source,test_scarce_cif_is_consumed_in_rule_priority_waterfall}` | E1/E5 adapter output had to equal retired norm-specific planner output (including legacy split actions and registry branches). | Persisted generic rules are authoritative: the first priority-matched rule owns an overlapping source, and scarce CIF is allocated in ascending rule priority. | Yes | No | Approved single canonical planner requirement. Independent calculations: 10 eligible x 2 = CIF 20 for the sole priority-1 owner; with CIF 60, priority 10 receives 10 x 5 = 50 then priority 20 receives (60 - 50) / 5 = 2, CIF 10. | Pass (6 focused generic execution and cap tests) |
| `apps/license/tests/test_replan_requests.py::test_license_api_reports_pending_then_current_revision_after_worker` | No API contract proved that a source update remains pending until the worker finishes; a pending durable request was also not executable because the task accepted only `queued`. | API exposes `REPLAN_PENDING` with `{source_revision: 1, planned_revision: 0, is_current: false}` before task execution and `CURRENT` with equal revisions only after the task succeeds. | Yes | Yes | Approved asynchronous flow. Source mutation increments 0 → 1; worker may mark applied revision 1 only when it planned started revision 1. | Environment gate pending: shared PostgreSQL test database was held by another runner; test collection reached 11 tests before setup contention. |

Reason: this was classified `FIXTURE_OR_TEST_DATA_DEFECT`, not a production regression. The production change already makes `force_plan` a selection/rebuild mode and preserves the caps. The fixture now carries the contract field required for the deterministic priority/waterfall algorithm; no expected numerical result was copied from a runtime result.

The deleted adapter-parity assertions are classified `OBSOLETE_TEST_AFTER_APPROVED_CHANGE` because their explicit authority was the retired E1/E5 implementation rather than a persisted rule contract. Coverage was not removed: unmatched sources, shortage behavior, deterministic first-match ownership, and the CIF-priority waterfall are now tested through the canonical generic executor. No production service changed for this migration.

The durable request status mismatch is classified `VALID_TEST_CODE_REGRESSION`: request creation deliberately returns `pending`, so dispatcher, recovery, and worker claim paths must all accept `pending` rather than silently treating the Celery message as complete. The API test does not claim a final plan before task execution; it invokes the task body and verifies revision equality before `CURRENT` is reported.
