Django==6.0.4
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-filter==25.2
django-cors-headers==4.9.0
django-extensions==4.1
django-redis==6.0.0
whitenoise==6.12.0
celery==5.6.3
redis==7.4.0
python-dotenv==1.2.2
openpyxl==3.1.5
reportlab==4.4.10
num2words==0.5.14
pypdf>=6.0.0
Pillow==12.2.0
# OCR + QR-decode fallback for scanned licence PDFs.
# Requires system: tesseract + poppler (OCR) and zbar (QR).
pytesseract==0.3.13
pdf2image==1.17.0
pyzbar==0.1.9
requests>=2.31.0
psycopg==3.3.3
docxtpl==0.20.2

# numpy is used by apps.core.scripts.calculation.optimize_milk_distribution
# (called from apps.license.models.get_glass_formers).
# 2.1+ has prebuilt wheels for Python 3.14.
numpy>=2.1,<3

# Master-Data Service client (ADR-001) — local package in this repo.
# Only needed when MDS_ENABLED=true. Install with:
#   pip install -e ../mds-client
# (Left as a comment so default installs don't require the local path.)
# mds-client
