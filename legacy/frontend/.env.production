# Production build — API calls use a relative path so each deployed domain
# calls its OWN backend (/api/ resolves to the same origin).
# Do NOT set VITE_API_URL here; leave it empty so the relative fallback is used.
VITE_API_URL=
